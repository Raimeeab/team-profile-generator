const generateHTML = (manager, engineer, intern) => {
    console.log(manager, engineer, intern) 
    const generateMangaer = (manager) =>{
        return `<li>${intern.getId} </>`
    }

    return `<div></div>` + `` 
}
module.exports = generateHTML;